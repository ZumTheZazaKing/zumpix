import { StyleSheet } from 'aphrodite';

export const searchStyles = StyleSheet.create({
    searchBar:{
        padding:"10px",
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        
    }
})